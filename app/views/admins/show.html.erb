<%= form_for(@Admin) do |f| %>
    <div class = "form_row">
      <label for="text">Name:</label>
      <%= f.text_field :name %>
    </div>
    <div class="actions">
      <%= f.submit "Create", :class => 'btn btn-info' %>
    </div>
<% end %>